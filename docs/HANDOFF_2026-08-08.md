# 交接文档：astrbot-plugin-engram（海马体）开发工作区

> 交接时间：2026-08-08
> 项目：astrbot-plugin-engram（Engram 类海马体长期记忆框架）v1.73
> 工作区：C:\Users\chiriu\.astrbot\data\plugins\astrbot_plugin_engram_core（开发=部署，单一目录）
> 目的：记录本轮整理 + 功能评估 + 修复结论，供后续开发者/Agent 无缝接续，避免重复调查。

---

## 1. 工作区结构（已确认事实）

- 开发目录已整体移入插件环境（2026-08-08，方案 A）：
  - `C:\Users\chiriu\.astrbot\data\plugins\astrbot_plugin_engram_core\` —— 开发 = 部署的单一目录，唯一 git 仓库在此
- 根目录 `archive/` 已被用户删除（内容：6/20 迁移遗留物 + 8/7 一次性脚本）
- 源目录 `C:\Users\chiriu\Documents\workspace\海马体\` 已删除（2026-08-11 确认）

## 2. 数据与部署（关键路径，勿动）

| 项目 | 路径 | 说明 |
|---|---|---|
| 活跃生产库 | `C:\Users\chiriu\.astrbot\data\hippocampus.db` | 83.6MB，持续写入（2026-08-08 仍在更新） |
| 部署目录（=开发目录） | `C:\Users\chiriu\.astrbot\data\plugins\astrbot_plugin_engram_core\` | 2026-08-08 整体移入，单一目录 |
| 插件默认库路径 | `data/hippocampus.db`（`hippocampus/config.py:6`） | 相对 AstrBot 数据目录解析 |
| 备份 | `.astrbot\data\backups\` | 插件 BackupManager 自动写入 |
| 插件 data/ | `C:\Users\chiriu\.astrbot\data\plugins\astrbot_plugin_engram_core\data\hippocampus.db` | **空库（0 engram）**，非活跃数据 |

已删除且不可恢复（不在 git 中）：`_migration_backup_20260620_021217`（61.5MB livingmemory 迁移前快照）、`hippocampus_migrated.db`、6/20 两个备份目录、`migrate_livingmemory.py`、`_arch_view.jpg`、`edit_v70.py`、`inspect_v70.py`。

## 3. 本轮已完成事项

### 3.1 工作区整理
- 迁移遗留物归档到 `archive/` → 用户确认后删除
- 删除空目录 `backups/` ×2（根目录 + 插件内）
- 清理 gitignore 缓存 `__pycache__` ×9、`tests/.pytest_cache` ×1（可重建）

### 3.2 文档归位
- `TODO.md` → `docs/TODO.md`（git rename 已暂存）
- `CHANGELOG.md` 保留根目录（用户决定不移；git 状态已理清，见 §4）

### 3.3 功能完整度评估（结论：完整度高）
- 31 个 `/mem` 命令全部注册（`main.py:279-458`），50+ 核心模块，WebUI 9 模块，i18n zh/en
- 源码扫描：0 个 NotImplementedError / 空函数体 / TODO 注释
- 测试：64 个 smoke（v08–v72，缺 v67）；最新 v65/66/68/70/71/72 全部通过

### 3.4 修复与文档更新
- `handlers/event/inject.py:200`：双横线叠加 bug 修复（safe_edit 修改，备份在 `C:\Users\chiriu\.irmia\backups\`）
- `README.md`：已知遗留段更新（删除已 ship 项、write_ops 标注未实现、引用改为 docs/TODO.md）
- `docs/TODO.md`：§2.10 状态 not started → 已修复 (2026-08-08)

## 4. 当前 git 状态（重要）

```
R  TODO.md -> docs/TODO.md          ← 暂存（docs 移动）
 M README.md                        ← 未暂存（已知遗留段更新）
 M handlers/event/inject.py         ← 未暂存（双横线修复）
 M tests/_smoke_v23.py              ← 未暂存（用户进行中改动，勿动）
 M tests/_smoke_v25.py              ← 未暂存（同上）
 M tests/_smoke_v26.py              ← 未暂存（同上）
 M tests/_smoke_v70.py              ← 未暂存（同上）
```

- CHANGELOG.md 已回到根目录且内容与 HEAD 一致，追踪干净
- 尚未 commit 任何内容

## 5. 项目功能状态与已知缺口

- 已 ship：v1.3 – v1.73 全部里程碑（召回/注入/persona/diary/relation/WebUI/i18n/备份/跨插件协调）
- not started：§2.2 write_ops 断电恢复、§2.3 Persona 定时重建 scheduler
- deferred：§2.4 graph 全 SQL 下推、§2.9 跨插件注入协调
- declined：§2.1 GC 判据、§2.5 CI、§2.6 合并 LLM、§2.8 system prompt 约定
- 详情见 `docs/TODO.md`（ROADMAP.md / TODO_summarization_B.md 已合并删除）

## 6. 测试与验证方法

```
python tests/_smoke_v65.py   # spread 路由
python tests/_smoke_v66.py   # session + MMR
python tests/_smoke_v68.py   # mem debug 路由
python tests/_smoke_v70.py   # 注入顺序（issue #8）
python tests/_smoke_v71.py   # re-injection 防御
python tests/_smoke_v72.py   # 跨插件协调（v1.73）
```

- 全部用临时库（tempfile.mkstemp），不碰生产数据
- Python 3.12；jieba 可选（未装自动降级 bigram）
- 测试零外部依赖（astrbot 用 stub 模拟）

## 7. 下一步建议

1. 提交改动（建议分两笔）：
   - `git add README.md handlers/event/inject.py` → `fix(inject): 日记双横线叠加 + docs 同步`
   - 4 个 test 文件改动按用户节奏单独提交
2. 源目录与旧部署备份已删除（2026-08-11 确认）；当前只有单一目录 `astrbot_plugin_engram_core`
3. 发布前候选：§2.3 Persona 定时重建（多用户价值上升）、§2.2 write_ops（生产部署前）

## 8. 风险与操作备忘

- 归档迁移物已删，无 git 恢复手段（不在版本控制内）
- 生产库在 `.astrbot\data\`；开发目录 `data/hippocampus.db` 是空库，改坏不影响生产但也不代表生产状态
- 修改 `.py` 必须走 `safe_edit`（backup + 语法检查 + 自动回滚），命令：`C:\Users\chiriu\.local\bin\safe_edit.cmd`
  - PowerShell 下用 `--old-env/--new-env` 或 `--old-file/--new-file` 传参（命令行引号会被 PowerShell 5.1 破坏）
- PowerShell 管道把中文传给 Python stdin 会损坏 → 用 unicode 转义，或 PowerShell 直接写文件
- 修改后必跑对应 smoke 测试再交付